import timeUtils from '../../../utils/time-utils';
import { module, test } from 'qunit';

module('Unit | Utility | time utils');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = timeUtils();
  assert.ok(result);
});
