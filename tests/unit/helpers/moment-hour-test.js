import { momentHour } from '../../../helpers/moment-hour';
import { module, test } from 'qunit';

module('Unit | Helper | moment hour');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = momentHour([42]);
  assert.ok(result);
});
